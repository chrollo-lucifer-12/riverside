import {ScissorsIcon, UploadIcon} from "lucide-react";
import ButtonLayout from "@/components/project-page/button-layout";

const EditButton = () => {
    return <ButtonLayout heading={"Edit"} description={"Creates clips and episodes."} icon={ScissorsIcon}/>
}

export default EditButton